<!--$('#favorite_buttons_<%#= @book.id %>').html("<%#= j(render "favorites/btn", book: @book) %>");-->

<!--$(".favorite-btn").html("<%#= escape_javascript(render partial: 'favorites/favorite-btn', locals: {book: @book}) %>");-->
<!--  // インスタンス変数は、favorites_controllerのcreateアクションで定義されたものが使用-->
<!--  // 以下の記述でも同じ挙動をします。html()以降では部分テンプレートを呼び出し-->
$("#book_<%= @book.id %> .favorite-btn").html("<%= j(render 'favorites/favorite-btn', book: @book) %>");